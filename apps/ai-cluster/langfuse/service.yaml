apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: langfuse
  labels:
    app: langfuse
    ztp-modernization-factory: "true"
spec:
  selector:
    app: langfuse
  ports:
    - port: 3000
      targetPort: 3000
      name: http
