apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: grafana
  labels:
    ztp-modernization-factory: "true"
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: OpenShift Prometheus
        uid: prometheus
        type: prometheus
        access: proxy
        url: https://thanos-querier.openshift-monitoring.svc:9091
        isDefault: true
        jsonData:
          httpHeaderName1: Authorization
          tlsSkipVerify: true
        secureJsonData:
          httpHeaderValue1: "Bearer PLACEHOLDER_AI_TOKEN"
        editable: true
      - name: Virt Cluster Prometheus
        uid: virt-prometheus
        type: prometheus
        access: proxy
        url: https://PLACEHOLDER_VIRT_THANOS_HOST
        isDefault: false
        jsonData:
          httpHeaderName1: Authorization
          tlsSkipVerify: true
        secureJsonData:
          httpHeaderValue1: "Bearer PLACEHOLDER_VIRT_TOKEN"
        editable: true
